All fully capitalized methods are top level 'master' priority. Other camel case methods are lower level utility type functions.


Need to find a way to add rendering on click with velocity line as well

rewrite line to be an arrow, maybe build custom svg?

On pause, maybe display all the internally stored velocity arrows? write function to take arbitrary body and draw the arrow rather than storing all the contents of the thing in a nested struct which is nasty


TBI will stand for To be implemented.



test black/white holes. Should they inherit from grav body?


Make scrolling functionality. Should allow smooth panning. Also a play pause to the physics. 

Add animation for instantiation. Investigate bloom on stars? add grav body texture, small enough masses should be terrestrial, larges masses should be more beefy.


Eventually refactor memory management to use smart pointers. It will vastly improve the readability of the code.


Add different types of instantiations -- black holes, dust, gas, planets, and stars. 


Will want to refactor Game System constants to include some dynamically changing user prefs as well

Clearly define terms -- fragments, dust, particles, (macro) bodies, planets, stars, etc.

function signature elements and struct members should be camelCase

intermediary calculated values and local variables should be snake case


UI will always be capitalized (because I decided this :D)


Probably should make the UIFont something that gets statically loaded in the background too? no reason to pass to the arguments?


User Input -> InputSystem -> UIState(inputState on inside) -> UISystem (update all elements) -> UIElement(mass slider, calculate new positions, and actual mass value) -> pass mass Value to UIState -> render at end?





NEED TO FIX THE MOUSE BUTTON HOLDING BUG RECAPTURING THE UI ELEMENT

Also need to add easy ui scaling?
Need to add radius slider
Need to fix mouse cursor centering of grabbing the slider
need to replace physics spawn time accumulator DTs with the true physics dt.


really need to add initial velocity vector instantiation

also add black hole class soon :D



Adding baseline behavior to blow up both macro colliding bodies as long as the heavier is no more than 40% lighter.




Want to rework collision dynamics so that spread doesn't produce that weird crescent shape.




Add restriction on creating 0 mass bodies unless in dev mode?

probably need to add testing for collision logic :(

Need to rework collision to ensure no bugs when instantiating bodies right on top of each other. 

Should either instantly accrete or correct their positions and put them at boundaries



Need to rework menus and scenes into enums most likely.

UISystem needs to own and manage the scenes around. That way I can put it in test scene easily.


Need to investigate private vs public vs protected methods bc they are bleeding over each other a bit




Changing playback speed causes dropped commands. Need to fit into the loop where input is polled at same time as the physics?

TBI change src to have UI as a full subfolder instead of this entities scenes nonsense?

TBI make build just build with the last build config and not run clean?


abstract the clickMe and slideMe into one method called interact, which will check the type and then dispatch at the local ui level?



NEED TO FIX: MACOS CMAKE LISTS FOR DEBUG IS BROKEN.